const hasKey = true;
const doorName = "Puerta del Castillo";

if (hasKey) {
    console.log(`Has abierto la ${doorName}.`);
} else {
    console.log(`Necesitas una llave para abrir la ${doorName}.`);
}