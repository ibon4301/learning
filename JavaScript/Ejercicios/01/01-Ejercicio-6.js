const hasKey = true;
const doorName = "Puerta antigua";

if (hasKey) {
    console.log(`Tienes la llave. Ahora puedes abrir la ${doorName}.`)
} else {
    console.log(`No tienes la llave. La ${doorName} está cerrada.`)
}