const playerLevel = 6;
const requiredLevel = 5;
const hasKey = true;

if (playerLevel >= requiredLevel && hasKey) {
    console.log("Puedes abrir la puerta especial.");
} else {
    console.log("No puedes abrir la puerta especial.");
}